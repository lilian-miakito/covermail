# Model profiles

No model profile is supported in Stages 0 and 1. The example address names a
placeholder profile only so the complete immutable address schema can be tested.
It must fail any future model-artifact preparation or compatibility self-test.
